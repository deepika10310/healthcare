import GreetingBoundary from '../components/GreetingBoundary';

const Home = () => {
  return (
    <div style={{ display: 'flex', justifyContent: 'center', alignItems: 'center', height: '100vh' }}>
      <GreetingBoundary />
    </div>
  );
};

export default Home;
